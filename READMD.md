If you are unable to run Redis/Docker, install Memurai from https://www.memurai.com/get-memurai

To run in background: nohup waitress-serve --listen=localhost:5000 application:app > BiddingBE.log 2>&1 &
